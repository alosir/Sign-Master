# 签到大师产品需求文档（PRD）

**文档版本**: v1.2.3  
**最后更新**: 2026-07-24

---

## 目录

1. [产品概述](#1-产品概述)
2. [功能需求](#2-功能需求)
3. [非功能需求](#3-非功能需求)
4. [用户故事](#4-用户故事)
5. [验收标准](#5-验收标准)
6. [技术约束](#6-技术约束)
7. [Roadmap](#7-roadmap)
8. [版本历史](#8-版本历史)
9. [术语表](#9-术语表)

---

## 1. 产品概述

### 1.1 项目名称

**签到大师**（开发包名：`com.alosir.task`，应用名：签到大师）

### 1.2 产品定位

一款面向 Android 用户的本地签到管理工具，帮助用户集中管理 APP、网站和自定义任务的签到/打卡行为，通过灵活的周期规则、签到提醒、数据统计与数据迁移，降低遗忘率并提升操作效率。

### 1.3 核心价值主张

- **集中管理**：所有签到任务统一入口，无需在多个应用间切换记忆
- **一键触达**：点击卡片即可启动目标 APP 或打开网站
- **周期灵活**：支持每天 / 每周 / 每月 / 自定义周期
- **提醒防漏**：可设置提醒时间，推送系统通知
- **状态可视**：今日、任务、统计、我的四页清晰分区
- **数据自主**：本地 Room 数据库存储，支持 JSON 导入导出

### 1.4 目标用户

- 需要在多个 APP 中每日签到领取奖励的用户
- 需要定期访问特定网站完成打卡的用户
- 将日常习惯、任务以清单形式管理的效率工具用户
- 偏好数据本地存储、不上云的 Android 用户

### 1.5 使用场景

| 场景 | 说明 |
|------|------|
| 每日 APP 签到 | 用户早晨打开签到大师，在「今日」页点击目标 APP 卡片，自动跳转并标记完成 |
| 每周/每月周期性任务 | 用户设置「每周一、三、五」或「每月 1 号」的签到项，系统只在到期日展示为待签到 |
| 自定义周期 | 用户设置「每 3 天」浇水、「每 2 周」健身等自定义任务 |
| 误操作恢复 | 用户不小心完成某项任务，可在已签到列表长按选择「重置」恢复 |
| 换机迁移 | 用户导出 JSON 备份，在新设备上导入恢复全部签到项与历史记录 |
| 提醒通知 | 用户为任务设置提醒时间，到达时间后系统推送通知，点击直达今日任务 |

---

## 2. 功能需求

### 2.1 签到项管理

#### 2.1.1 类型支持

| 类型 | 常量值 | 触发行为 |
|------|--------|----------|
| APP 签到 | `CheckinType.APP = 0` | 完成时启动已安装应用 |
| 网站签到 | `CheckinType.WEBSITE = 1` | 完成时通过浏览器打开网址 |
| 其他任务 | `CheckinType.OTHER = 2` | 直接标记为已完成 |

#### 2.1.2 APP 类型添加流程

- 入口：`MainActivity` 的 FAB → `AddCheckinBottomSheet` → 选择 APP 类型 → 选择应用
- 展示设备已安装应用列表（排除自身和无可启动 Intent 的应用），按应用名升序排列
- 顶部提供搜索框，实时过滤应用名，过滤不区分大小写
- 用户选择应用后进入周期与提醒设置
- 保存时创建 `CheckinItem`，`type = APP`，`packageName` 为所选包名

#### 2.1.3 网站类型添加流程

- 入口同上，选择网站后输入名称、URL、描述
- URL 自动补全：若用户未输入 `http://` 或 `https://` 前缀，自动补全为 `https://`
- 保存时调用 `IconManager.saveDefaultIcon` 生成默认网站图标文件，`iconPath` 指向该文件

#### 2.1.4 其他类型添加流程

- 入口同上，选择其他任务后输入名称、描述
- 保存时同样生成默认图标文件

#### 2.1.5 周期配置

- 周期类型：`DAY(0)` / `WEEK(1)` / `MONTH(2)`
- 每天模式：每 N 天
- 每周模式：选择周一 ~ 周日任意多天；或每 N 周
- 每月模式：选择 1 ~ 31 号及「最后一天」；或每 N 个月
- 可设置跳过法定节假日、跳过周末
- 实时显示周期描述文本
- 默认周期为「每天」

#### 2.1.6 提醒设置

- 添加/编辑任务时可选择是否开启提醒
- 提醒时间格式：`HH:mm`
- 保存后注册 AlarmManager 精确提醒
- 创建带提醒任务时自动检查并申请通知权限

#### 2.1.7 图标处理

- APP：不保存图标文件，运行时从 `PackageManager` 动态获取
- 网站/其他：保存默认占位图标到应用私有目录 `/files/icons/`

### 2.2 主页面

#### 2.2.1 底部导航

- 今日：`TodayFragment`
- 任务：`TasksFragment`（含未签到 / 已签到两个 Tab）
- 统计：`StatisticsFragment`
- 我的：`ProfileFragment`

#### 2.2.2 添加按钮

- 首页右下角悬浮按钮（FAB）
- 点击展开 `AddCheckinBottomSheet`

#### 2.2.3 今日页

- 展示当天待签到任务
- 展示当天已完成任务（灰色卡片，显示完成时间与描述）
- 支持右滑快速签到
- 支持下拉刷新

#### 2.2.4 任务页

- 未签到 Tab：展示所有当前待签到任务（含今天及未来到期）
- 已签到 Tab：展示所有历史签到记录
- 已签到列表显示任务描述
- 支持下拉刷新

#### 2.2.5 统计页

- 顶部展示本月完成率、本周完成率
- 类型分布饼图（APP / 网站 / 其他）
- 月度日历：标记「全部完成 / 部分完成」日期
- 连续签到天数排行（不限制数量）
- 支持下拉刷新

#### 2.2.6 我的页

- 导出数据按钮
- 导入数据按钮
- 通知权限状态卡片：显示是否开启，未开启时点击打开系统设置

### 2.3 签到状态管理

#### 2.3.1 状态枚举

```kotlin
enum class CheckinStatus {
    PENDING,    // 可签到
    COMPLETED   // 已完成
}
```

#### 2.3.2 状态判定逻辑

`CycleCalculator.isCheckinAvailable(item, date)` 核心逻辑：

```text
if date 是节假日/周末且任务设置跳过: return false
if lastCheckinDate == date: return false
if lastCheckinDate == null: return matchesCycleDate(item, date)

根据 cycleType / cycleValue 与 lastCheckinDate 计算是否到达下一签到日
```

- 所有日期比较忽略时分秒，仅比较年月日
- 不同周期的「下次可签到日」按基准日计算

#### 2.3.3 签到记录

每次成功签到插入一条 `CheckinRecord`：
- `itemId`: 关联签到项 ID
- `checkinDate`: 当前日期（yyyy-MM-dd）
- `checkinTime`: 当前时间戳（yyyy-MM-dd HH:mm:ss）
- `isAuto`: 是否自动签到，默认 false
- `status`: 状态，默认 `STATUS_SUCCESS = 1`

同时更新 `CheckinItem.last_checkin_date` 为当前日期。

#### 2.3.4 每日刷新 Worker

- `DailyCheckinRefreshWorker` 每日 0:00 执行
- 清理 7 天前的 `checkin_records` 记录，避免数据无限增长
- 由 `CheckinApplication.onCreate` 通过 `CheckinResetScheduler.schedule` 注册

### 2.4 数据管理

#### 2.4.1 数据导出

- 入口：`ProfileFragment` 导出数据按钮
- 导出所有 `checkin_items` 和 `checkin_records` 为 JSON
- 文件名格式：`checkinmaster_export_yyyyMMdd_HHmmss.json`
- 保存路径：系统 `Download` 文件夹

#### 2.4.2 数据导入

- 入口：`ProfileFragment` 导入数据按钮
- 使用 `ActivityResultContracts.GetContent()` 选择文件
- 读取 JSON 后，按 item ID 去重：已存在的 item 跳过
- 导入记录直接插入，失败则忽略单条
- 导入完成后 Toast 提示成功 / 重复 / 失败数量

### 2.5 提醒通知

#### 2.5.1 提醒调度

- `ReminderScheduler` 使用 `AlarmManager` + `PendingIntent`
- 到达时间触发 `ReminderReceiver`
- `ReminderReceiver` 调用 `NotificationHelper` 推送通知

#### 2.5.2 通知行为

- 通知标题为任务名称
- 通知内容为任务描述或默认文案
- 点击通知打开 `MainActivity` 并切换到「今日」页

#### 2.5.3 通知权限

- Android 13+ 申请 `POST_NOTIFICATIONS`
- `NotificationPermissionHelper` 检测系统通知开关状态
- 被拒绝时引导跳转系统设置

### 2.6 自动化签到（框架已就绪，UI 未接入）

#### 2.6.1 无障碍服务

- `AutomationService` 继承 `AccessibilityService`
- 监听事件：点击、聚焦、窗口状态变化、窗口内容变化
- 录制时通过 `ScriptRecorder` 记录用户操作
- 回放时通过 `performAction` 执行 click / long_click / swipe / text / back / home / wait 等动作

#### 2.6.2 动作信息

`ActionInfo` 数据结构：
- `type`: 动作类型
- `target`: 目标定位信息（id / text / desc / focus）
- `delay`: 执行前等待延迟
- `duration`: 滑动/等待持续时间
- `text`: 输入文本
- `startX/startY/endX/endY`: 滑动坐标
- `timestamp`: 时间戳

#### 2.6.3 脚本存储

- 脚本保存在 `automation_scripts` 表
- 字段：`id`, `name`, `item_id`, `actions`（JSON 字符串）, `created_at`
- 通过 `ScriptParser` 进行 JSON 与 Kotlin 对象的互转

#### 2.6.4 当前限制

- 无障碍服务需要在系统设置中手动开启，应用内无引导入口
- 录制/编辑/绑定脚本的 UI 尚未实装
- Android 10+ 限制后台启动 Activity，自动签到需应用处于前台
- 无法处理验证码、随机布局等反自动化机制

---

## 3. 非功能需求

### 3.1 性能

| 指标 | 目标 |
|------|------|
| 应用冷启动时间 | < 3 秒 |
| Tab 切换响应 | < 100ms |
| 搜索过滤响应 | < 100ms |
| 列表滚动 | 60 FPS |
| 数据库查询 | < 50ms |
| 内存占用 | < 100MB |
| APK 大小 | < 20MB |

### 3.2 兼容性

| 项目 | 规格 |
|------|------|
| 最低 Android 版本 | Android 5.0 (API 21) |
| 推荐 Android 版本 | Android 8.0 (API 26) 及以上 |
| 目标 SDK | API 34 |
| 屏幕尺寸 | 4.7" ~ 12.9" |
| 屏幕方向 | 竖屏优先 |

### 3.3 安全与隐私

- 所有数据存储在应用私有目录或 Room 数据库中，不上传服务器
- 导出 JSON 文件保存至系统 Download 文件夹
- 权限遵循最小化原则
- 自动化操作需用户主动开启无障碍服务
- Release 签名文件与 keystore.properties 不进入版本控制

### 3.4 无障碍

- 图标与状态指示器包含 `contentDescription`
- 自动化服务为视障用户操作目标应用提供技术基础

---

## 4. 用户故事

### US-1 集中管理签到

**作为** 拥有多个签到 APP 的用户，  
**我想要** 在一个应用中集中查看所有待签到任务，  
**以便** 不会遗漏任何每日签到。

### US-2 灵活周期配置

**作为** 有周期性任务的用户，  
**我想要** 设置「每 2 天」「每周一三五」或「每月 1 号」的签到周期，  
**以便** 任务只在真正到期时提醒我去完成。

### US-3 一键快速签到

**作为** 追求效率的用户，  
**我想要** 在「今日」页点击卡片就能直接打开目标 APP 或网站，  
**以便** 减少查找和切换应用的时间。

### US-4 误操作恢复

**作为** 不小心提前点击了签到的用户，  
**我想要** 在已签到列表长按卡片选择「重置」，  
**以便** 将任务恢复为待签到状态。

### US-5 数据迁移

**作为** 更换手机的用户，  
**我想要** 一键导出所有签到项和历史记录，并在新设备导入，  
**以便** 快速恢复使用环境。

### US-6 提醒通知

**作为** 容易忘记签到的用户，  
**我想要** 为任务设置提醒时间并接收系统通知，  
**以便** 不会错过签到时间。

### US-7 自动化签到（未来）

**作为** 需要重复点击完成签到的用户，  
**我想要** 录制目标 APP 内的操作并自动回放，  
**以便** 进一步减少手动操作。

---

## 5. 验收标准

### 5.1 功能验收

| 功能 | 验收标准 |
|------|----------|
| 添加 APP 签到 | 可从已安装应用列表搜索并选择应用，保存后出现在今日/任务页 |
| 添加网站签到 | 输入网址后自动补全 https://，保存后完成时打开浏览器 |
| 添加其他任务 | 输入名称即可保存，完成时直接标记 |
| 周期计算 | 每天/每周/每月/自定义严格按规则判断，不误提前标记为待签到 |
| 提醒通知 | 设置提醒时间后到达时间推送通知，点击打开今日页 |
| 通知权限 | Android 13+ 正确申请权限，可引导至系统设置 |
| 状态显示 | PENDING 与 COMPLETED 在卡片背景色、状态指示器颜色上区分明显 |
| 今日页 | 只展示当天待签到与当天已完成 |
| 任务页 | 未签到含今天及未来到期，已签到展示历史记录 |
| 统计页 | 日历正确标记 FULL/PARTIAL，排行不限制数量 |
| 我的页 | 可导出/导入数据，显示通知权限状态 |
| 编辑 | 异步保存不阻塞 UI；修改周期后清空该 item 历史记录 |
| 删除 | 二次确认后删除 item，关联记录和脚本级联清除 |
| 导入/导出 | JSON 文件格式正确，导入按 ID 去重，导出包含全部数据 |
| 每日刷新 | WorkManager 每日 0:00 清理 7 天前旧记录 |
| 深色模式 | Android 10+ 自动适配深色主题 |

### 5.2 性能验收

| 指标 | 标准 |
|------|------|
| 冷启动 | < 3 秒 |
| Tab 切换 | < 100ms |
| 搜索响应 | < 100ms |
| 列表滚动 | 流畅，无明显掉帧 |
| 内存占用 | < 100MB |
| APK 大小 | < 20MB |

### 5.3 兼容性验收

| 设备/系统 | 标准 |
|-----------|------|
| Android 5.0+ | 可安装运行 |
| Android 8.0+ | 推荐使用，所有功能正常 |
| 手机/平板 | 适配 4.7" ~ 12.9" 屏幕 |

---

## 6. 技术约束

### 6.1 开发环境

| 工具 | 版本 |
|------|------|
| Android Studio | 2023.1+ |
| JDK | 17 |
| Android SDK | API 34 |
| Gradle | 8.2 |
| Kotlin | 1.9.20 |

### 6.2 技术栈

| 层次 | 技术 |
|------|------|
| 语言 | Kotlin 1.9.20 |
| 架构 | MVVM + Repository |
| UI | Material Components 1.11.0 (Material 3) |
| 数据库 | Room 2.6.1 |
| 异步 | Kotlin Coroutines 1.7.3 |
| 后台 | WorkManager 2.9.0 + AlarmManager |
| JSON | Gson 2.10.1 |
| 构建 | Gradle Kotlin DSL |

### 6.3 关键依赖

```kotlin
androidx.core:core-ktx
androidx.appcompat:appcompat
com.google.android.material:material
androidx.constraintlayout:constraintlayout
androidx.recyclerview:recyclerview
androidx.fragment:fragment-ktx
androidx.lifecycle:lifecycle-viewmodel-ktx
androidx.lifecycle:lifecycle-livedata-ktx
androidx.lifecycle:lifecycle-runtime-ktx
androidx.room:room-runtime / room-ktx / room-compiler
androidx.work:work-runtime-ktx
org.jetbrains.kotlinx:kotlinx-coroutines-android
com.google.code.gson:gson
```

### 6.4 权限

| 权限 | 用途 | 必需 |
|------|------|------|
| `QUERY_ALL_PACKAGES` | 列举已安装应用 | 是 |
| `INTERNET` | 网站签到 | 是 |
| `POST_NOTIFICATIONS` | 提醒通知（Android 13+） | 否 |
| `SCHEDULE_EXACT_ALARM` | 精确提醒 | 否 |
| `WRITE_EXTERNAL_STORAGE` | 导出到 Download（Android 9 及以下） | 否 |
| `BIND_ACCESSIBILITY_SERVICE` | 自动化签到 | 否 |

---

## 7. Roadmap

### v1.0.x — 稳定性与体验优化

目标：修复现有功能 bug，打磨细节体验。

- v1.0.1：修复日历状态在跨天场景下的异常；优化连续签到排行排序
- v1.0.2：优化导入导出失败提示；修复节假日判断边界问题
- v1.0.3：优化通知权限引导流程；修复深色模式下部分控件颜色

### v1.1.0 — 版本更新与图标优化

目标：提供应用内版本管理与更新能力。

- 版本更新页面，展示面向用户的更新日志
- GitHub Release 检查与 APK 下载安装
- 我的页「当前版本」入口卡片
- 统一四页顶部边距
- 更换 APP 桌面图标

### v1.1.1 / v1.1.2 — 修复与优化

- v1.1.1：无 GitHub Release/APK 时提示「已是最新版本」
- v1.1.2：修复启动闪退；补全 VersionUpdateActivity 声明；增强桌面角标容错

### v1.2.0 — 任务终止与后台保活

目标：增强任务生命周期管理与后台可用性。

- 任务「终止」逻辑：按次数 / 按日期 / 手动终止
- 后台保活服务与自启动引导
- 统一任务详情弹窗
- 已签到列表左滑改为详情
- 统计页连续签到排行显示描述

### v1.3.0 — 自动化签到 UI

目标：将已有的无障碍服务框架通过 UI 暴露给用户。

- 在「我的」页增加「自动化服务」入口
- 为任务绑定/录制自动化脚本
- 脚本列表管理与删除
- 录制前引导用户开启无障碍服务

### v1.4.0 — 数据增强

目标：让数据更有价值，支持多维度查看。

- 按任务查看历史签到记录日历
- 导出 CSV / Excel 格式
- 支持云备份到 WebDAV / Google Drive（可选）

### v1.5.0 — 小组件与快捷方式

目标：提升不解锁手机时的操作效率。

- 桌面 Widget：展示今日待签到数量与快捷完成
- 任务长按快捷菜单（Shortcuts）
- 通知栏常驻快捷入口（可选）

### v2.0.0 — 大改版（待定方向）

目标：基于用户反馈进行全面重构或重大方向调整。

可能方向：
- 全新 Compose UI 重写
- 多端同步（Wear OS / 桌面端）
- 社区共享签到模板
- AI 智能周期推荐

> v2.0.0 的启动需经过充分评估，不在当前 Roadmap 的确定排期内。

---

## 8. 版本历史

| 版本 | 日期 | 关键改动 |
|------|------|----------|
| v1.2.3 | 2026-07-24 | 移除我的页忽略电池优化；任务详情弹窗背景适配深色模式；按日期日历改为年月左上、<◎>按钮右上，并支持左右滑动 |
| v1.2.2 | 2026-07-24 | 任务详情弹窗背景改为纯白；图标与列表页一致（APP 显示应用图标）；待签到任务增加「跳过」按钮；移除我的页开机自启动与保活开关，优化电池优化入口 |
| v1.2.1 | 2026-07-24 | 优化任务详情弹窗样式与按钮布局；结束签到设置移到提醒时间下方；修复结束签到按钮样式；按次数组件左右各占 50%；日历支持滑动切换与月份边界限制；统计页日历支持滑动与边界限制 |
| v1.2.0 | 2026-07-18 | 新增任务终止逻辑（按次数/按日期/手动）；新增后台保活与自启动引导；统一任务详情弹窗；已签到列表左滑改为详情；统计页排行显示描述 |
| v1.1.2 | 2026-07-17 | 修复应用启动闪退；补全 VersionUpdateActivity 声明；增强桌面角标容错 |
| v1.1.1 | 2026-07-17 | 优化检查更新逻辑：无 GitHub Release/APK 时提示「已是最新版本」 |
| v1.1.0 | 2026-07-17 | 新增版本更新页面与 GitHub 检查更新；我的页新增当前版本卡片；统一四页顶部边距；更换 APP 桌面图标 |
| v1.0.2 | 2026-07-13 | 统一统计页标题为 Toolbar 样式；我的页新增居中的版本号与更新日期；更换 APP 桌面图标 |
| v1.0.1 | 2026-07-13 | 新增桌面图标数字角标：实时显示「今日」待签到数量，全部完成后自动消失 |
| v1.0.0 | 2026-07-13 | 全新重构发布：底部导航四页架构、周期选择器升级、签到提醒、统计页、通知权限、导出到 Download、Release 签名 |

---

## 9. 术语表

| 术语 | 说明 |
|------|------|
| 签到项 (CheckinItem) | 用户需要签到的项目，包含名称、类型、周期、最后签到日期等 |
| 签到记录 (CheckinRecord) | 每次签到产生的记录，关联签到项，包含日期、时间、是否自动、状态 |
| 周期 (Cycle) | 签到重复规则，由周期类型（天/周/月）和周期值组成 |
| 周期状态 (CheckinStatus) | 签到项当前状态：`PENDING`（可签到）或 `COMPLETED`（已完成） |
| 刷新日 | 周期判断的基准日期：DAY 为当天，WEEK 为周一，MONTH 为 1 号 |
| 重置 | 将已签到状态恢复为待签到，清空当日记录和最后签到日期 |
| CASCADE | 数据库外键级联删除，删除 item 时自动删除关联记录和脚本 |
| 无障碍服务 | Android AccessibilityService，用于录制和回放用户界面操作 |
| WorkManager | Android 后台任务调度组件，用于每日 0:00 清理旧签到记录 |
| AlarmManager | Android 精确闹钟调度组件，用于提醒通知 |

---

**文档结束**
